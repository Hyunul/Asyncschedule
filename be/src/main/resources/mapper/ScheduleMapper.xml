<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aisg.devlogix.mapper.ScheduleMapper">

    <select id="getAllSchedule" resultType="java.util.Map">
        SELECT s.* 
          FROM schedule s
          LEFT JOIN user u ON s.user = u.username
          LEFT JOIN user_group ug ON u.id = ug.user_id
          LEFT JOIN group_table g ON ug.group_id = g.id
        <where>
           AND s.date <![CDATA[>=]]> #{startDate}
           AND s.date <![CDATA[<=]]> #{endDate}
          <if test='gubun != null and gubun == "s"'>
            AND s.user = #{user}
          </if>
          <if test='group_id != null and group_id != ""'>
            AND g.name = #{group_id}
          </if>
        </where>
    </select>

    <select id="getRecomSchedule" resultType="java.util.Map">
        SELECT distinct date, time 
          FROM schedule
        <where>
            AND date BETWEEN #{startDate} AND #{endDate}
            AND time BETWEEN '19:00:00' AND '21:00:00'
        </where>
    </select>

</mapper>