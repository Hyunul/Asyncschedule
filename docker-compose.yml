version: "3.8"

services:
  async-be:
    build:
      context: ./async-be
      dockerfile: Dockerfile
    image: hyunul/async-be:latest
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: prod
    restart: always
